//?.
//[""]
//first check if array has data then access data 
///users && users[0] && users[0].name
///users?.[0]?.name
function getUserData(age, name) {
    console.log(age, name);
}
getUserData(21);
